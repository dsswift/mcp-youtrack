# YouTrack MCP Server Configuration
# Copy this file to .env and fill in your values

# Required: YouTrack instance URL (without trailing slash)
YOUTRACK_URL=https://youtrack.example.com

# Required: YouTrack permanent token
# Generate at: YouTrack > Profile > Account Security > New token
# Select "YouTrack" scope for full issue access
YOUTRACK_TOKEN=perm:your-token-here

# Optional: Default project short name for issue creation
# YOUTRACK_DEFAULT_PROJECT=MYPROJECT

# Optional: Request timeout in seconds (default: 30)
# YOUTRACK_TIMEOUT=30

# Optional: SSL verification (default: true)
# Set to "false" for self-signed or internal CA certificates
# YOUTRACK_VERIFY_SSL=true
